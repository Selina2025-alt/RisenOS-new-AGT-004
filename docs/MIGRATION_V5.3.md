# MIGRATION_V5.3

AI-Content-Factory 迁移任务、草稿、内容、图片、模板、编辑历史和可恢复版本；剥离账号、凭据、发布记录、链接、监测和效果字段。

流程：

```text
扫描 → 字段分类 → 平台字段剥离 → 内容/图片哈希
→ 版本映射 → 版权状态 → dry-run → 数量/哈希对账 → 正式导入
```

无法还原完整历史版本的记录必须标记 `legacyImported=true`，不得伪造 Lineage。正式导入前必须保留源数据只读副本和回滚清单。
